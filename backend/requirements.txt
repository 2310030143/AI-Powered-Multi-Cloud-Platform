# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Settings & environment
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Security & auth (Phase 2)
passlib==1.7.4
bcrypt==4.0.1            # pinned: passlib 1.7.4 is incompatible with bcrypt >= 4.1 metadata
python-jose[cryptography]==3.3.0
cryptography==50.0.1     # Fernet encryption for stored cloud credentials
email-validator==2.3.0   # Pydantic EmailStr
python-multipart==0.0.32 # multipart/form-data uploads

# HTTP client (OAuth flows)
httpx==0.28.0

# Google Drive
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0

# S3-compatible object storage (Backblaze B2 / AWS S3 / MinIO)
boto3==1.35.80

# Document processing (Phase 3)
pypdf==5.1.0
python-docx==1.1.2
pandas==2.2.3
pillow==11.0.0

# OCR (Phase 3)
pytesseract==0.3.13
pdf2image==1.17.0

# Table extraction (Phase 3)
pdfplumber==0.11.4

# Embeddings & AI (Phase 4-6)
openai==1.57.2
tiktoken==0.8.0

# Vector DB (Phase 4)
qdrant-client==1.12.1

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
